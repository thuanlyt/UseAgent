# Project-scoped defaults for Codex multi-agent workflows.
# Model and sandbox are intentionally inherited from the parent session.

[agents]
enabled = true
max_concurrent_threads_per_session = 6
interrupt_message = true
